<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.airlift</groupId>
        <artifactId>airbase</artifactId>
        <version>395</version>
    </parent>

    <groupId>dev.jaaj.trino</groupId>
    <artifactId>trino-sqlite</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>trino-plugin</packaging>
    <description>Trino - SQLite connector</description>

    <properties>
        <air.java.version>25.0.1</air.java.version>
        <project.build.targetJdk>25</project.build.targetJdk>
        <dep.trino.version>483</dep.trino.version>
        <dep.airlift.version>439</dep.airlift.version>
        <dep.sqlite-jdbc.version>3.53.4.0</dep.sqlite-jdbc.version>
        <!-- Neither airbase nor the airlift bom manages units; this is the version Trino 483 pins. -->
        <dep.airlift-units.version>1.13</dep.airlift-units.version>

        <!--
          trino-main's BlockEncodingSimdSupport touches jdk.incubator.vector, an incubator
          module that is not on the module path unless requested explicitly. Trino's own
          root pom adds the same flag to its test JVM arguments.
        -->
        <air.test.jvm.additional-arguments>--add-modules=jdk.incubator.vector</air.test.jvm.additional-arguments>

        <!--
          Airbase's default PMD ruleset references the pre-7.x resource path
          /rulesets/java/basic.xml, which does not exist in the PMD 7.26.0 that this
          build resolves; the pmd goal fails before analyzing any source. Trino's own
          root pom disables this same check (air.check.skip-pmd=true) for the same
          reason.
        -->
        <air.check.skip-pmd>true</air.check.skip-pmd>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.airlift</groupId>
                <artifactId>bom</artifactId>
                <version>${dep.airlift.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>

        <dependency>
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
            <!--
              airbase and trino-base-jdbc both depend on the "classes" classifier of guice
              (a jar without the license and module-info resources that otherwise collide
              across guice's own published artifacts); an unclassified guice dependency
              pulls in a second, duplicate copy of every class and fails the duplicate
              finder. The classifier is also what airbase's banned-dependencies enforcer
              rule requires.
            -->
            <classifier>classes</classifier>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>configuration</artifactId>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>log</artifactId>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>units</artifactId>
            <version>${dep.airlift-units.version}</version>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-base-jdbc</artifactId>
            <version>${dep.trino.version}</version>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-filesystem</artifactId>
            <version>${dep.trino.version}</version>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-filesystem-s3</artifactId>
            <version>${dep.trino.version}</version>
        </dependency>

        <!-- SqliteClient maps identifiers through IdentifierMapping, from trino-base-jdbc's own transitive dependency. -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-plugin-toolkit</artifactId>
            <version>${dep.trino.version}</version>
        </dependency>

        <dependency>
            <groupId>jakarta.inject</groupId>
            <artifactId>jakarta.inject-api</artifactId>
        </dependency>

        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${dep.sqlite-jdbc.version}</version>
        </dependency>

        <!--
          trino-base-jdbc pulls these in at compile scope, but they are also compile-scope
          dependencies of trino-spi itself, so the trino-maven-plugin SPI check requires
          them to be provided here: bundling a second copy would duplicate classes the
          plugin classloader already exposes from the SPI at runtime.
        -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>slice</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api-incubator</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-common</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-context</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-spi</artifactId>
            <version>${dep.trino.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.locationtech.jts</groupId>
            <artifactId>jts-core</artifactId>
            <!-- Not managed by airbase or the airlift bom; pinned to the version trino-spi:483 resolves. -->
            <version>1.20.0</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>configuration-testing</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-main</artifactId>
            <version>${dep.trino.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- QueryAssertions.isFullyPushedDown and PlanMatchPattern live in trino-main's test jar. -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-main</artifactId>
            <version>${dep.trino.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-testing</artifactId>
            <version>${dep.trino.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- MinIO container for the S3 integration test. -->
        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-testing-containers</artifactId>
            <version>${dep.trino.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- TestSqliteS3Integration checks DockerClientFactory directly to skip when Docker is unavailable. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <!-- Not managed by airbase or the airlift bom; pinned to the version trino-testing-containers:483 resolves. -->
            <version>2.0.5</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>ca.vanzyl.provisio.maven.plugins</groupId>
                    <artifactId>provisio-maven-plugin</artifactId>
                    <version>1.1.3</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>io.trino</groupId>
                <artifactId>trino-maven-plugin</artifactId>
                <version>20</version>
                <extensions>true</extensions>
            </plugin>

            <!--
              trino-testing pulls in trino-exchange-filesystem (test scope), whose azure,
              AWS and reactor-netty client dependencies each carry their own netty release,
              and docker-java's transport clashes with the httpclient5 copy of the public
              suffix list. Both pairs are scoped exceptions here, following the pattern
              Trino's own root pom uses for the same kind of transitive-dependency overlap
              (see io.trino:trino:pom:483, duplicate-finder-maven-plugin configuration).
            -->
            <plugin>
                <groupId>org.basepom.maven</groupId>
                <artifactId>duplicate-finder-maven-plugin</artifactId>
                <configuration>
                    <exceptions combine.children="append">
                        <exception>
                            <conflictingDependencies>
                                <dependency>
                                    <groupId>io.netty</groupId>
                                    <artifactId>netty-codec-base</artifactId>
                                </dependency>
                                <dependency>
                                    <groupId>io.netty</groupId>
                                    <artifactId>netty-codec</artifactId>
                                </dependency>
                            </conflictingDependencies>
                            <packages>
                                <package>io.netty.handler.codec</package>
                                <package>io.netty.handler.codec.base64</package>
                                <package>io.netty.handler.codec.bytes</package>
                                <package>io.netty.handler.codec.json</package>
                                <package>io.netty.handler.codec.serialization</package>
                                <package>io.netty.handler.codec.string</package>
                            </packages>
                        </exception>
                        <exception>
                            <conflictingDependencies>
                                <dependency>
                                    <groupId>com.github.docker-java</groupId>
                                    <artifactId>docker-java-transport-zerodep</artifactId>
                                </dependency>
                                <dependency>
                                    <groupId>org.apache.httpcomponents.client5</groupId>
                                    <artifactId>httpclient5</artifactId>
                                </dependency>
                            </conflictingDependencies>
                            <resources>
                                <resource>org/publicsuffix/list/effective_tld_names.dat</resource>
                            </resources>
                        </exception>
                    </exceptions>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
